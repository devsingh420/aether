# Aether Produce Frontend Environment Variables

# Backend API URL (include /api/v1 path)
VITE_API_URL=http://localhost:3000/api/v1

# Stripe Publishable Key (get from Stripe Dashboard)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key

# Optional: LINE Login Channel ID (for frontend redirect)
VITE_LINE_CHANNEL_ID=your_line_channel_id
